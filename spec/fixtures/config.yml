extras:
  -
    yaml: "fixtures/extras.yaml" 

components:
  test:
    filename: "fixtures/test.rb"
    output: "/tmp/test.json"
  test_two:
    filename: "fixtures/heattest.rb"
    output: "/tmp/test_heat.json"
